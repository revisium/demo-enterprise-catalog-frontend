# Dev server port (react-router dev / Vite).
REACT_APP_PORT=5173

# This frontend is mock-first for the current stage. Add backend/Revisium URLs
# here only after the page contracts are stable enough to connect.
