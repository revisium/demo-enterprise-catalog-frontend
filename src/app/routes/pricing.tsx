import { PlaceholderPage } from 'src/shared/ui';

export default function PricingRoute() {
  return (
    <PlaceholderPage
      title="Pricing and price books"
      summary="Review regional server prices, currencies, contract terms, setup fees, add-ons, discounts, and price-book revisions."
    />
  );
}
