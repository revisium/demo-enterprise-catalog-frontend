import { PlaceholderPage } from 'src/shared/ui';

export default function CustomerPortalRoute() {
  return (
    <PlaceholderPage
      title="Customer portal"
      summary="Authorized users will manage saved plans, quote requests, favorites, API keys, and organization billing profiles."
    />
  );
}
